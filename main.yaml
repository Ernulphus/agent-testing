# Dataset Configuration
data_path: /data/xiuyuan/workspace/tele-safety/attack/questions_with_category.json

# Target Model Configuration
target_model_type: openai          # Options: openai, local
target_model_name: sonar-reasoning
target_model_path: null            # Not used for API-based models

# Auxiliary Model Configuration (for attacks like PAIR)
attack_model_type: openai
attack_model_name: sonar-reasoning
attack_model_path: null

eval_model_type: NULL

# API Configuration (Perplexity)
api_key: ${PERPLEXITY_API_KEY}
base_url: https://api.perplexity.ai

# Results Configuration
res_save_path: ./results/sonar_attack_result.jsonl
